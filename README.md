# 🎉 קהוט חוגגים 80 לסבא צדוק!

משחק שאלון בסגנון Kahoot, מותאם אישית ליום ההולדת ה-80 של סבא צדוק.
בנוי ב-HTML/CSS/JavaScript עם סנכרון בזמן אמת דרך **Firebase Realtime Database**.

## 📁 הקבצים
- `index.html` – מבנה הדף (דף אחד: בית / מנחה / שחקן)
- `style.css` – עיצוב חגיגי, קונפטי, כותרת 80, כפתורי צבעים
- `app.js` – כל לוגיקת המשחק והחיבור ל-Firebase

---

## 🔧 שלב 1 – הקמת Firebase (חינם, 5 דקות)

1. היכנסו ל-<https://console.firebase.google.com> וצרו פרויקט חדש.
2. בתפריט הצד בחרו **Build → Realtime Database → Create Database**.
   - בחרו מיקום, ואז **Start in test mode** (מאפשר קריאה/כתיבה לחגיגה).
3. בעמוד הראשי לחצו על אייקון **</>** (Web) כדי לרשום אפליקציית Web, ותקבלו אובייקט `firebaseConfig`.
4. פתחו את `app.js` והדביקו את הערכים במקום המסומן:

```js
const firebaseConfig = {
  apiKey:      "...",
  authDomain:  "...",
  databaseURL: "https://XXX-default-rtdb.firebaseio.com",  // ⚠️ חובה!
  projectId:   "...",
  storageBucket: "...",
  messagingSenderId: "...",
  appId:       "..."
};
```

> **חשוב:** ודאו שקיים שדה `databaseURL`. בלעדיו הסנכרון לא יעבוד.

### כללי אבטחה (Test mode)
ל-`test mode` יש כללים פתוחים שמספיקים לערב אחד. אם רוצים לחדד, אפשר להשתמש בכללים אלה תחת **Rules**:

```json
{
  "rules": {
    "games": {
      "$pin": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```

---

## 🚀 שלב 2 – העלאה ל-GitHub Pages

1. צרו מאגר (repository) חדש ב-GitHub, למשל `saba-80`.
2. העלו את שלושת הקבצים: `index.html`, `style.css`, `app.js`.
3. בהגדרות המאגר: **Settings → Pages → Source → Deploy from a branch → main / (root) → Save**.
4. אחרי דקה תקבלו כתובת כמו: `https://<שם-משתמש>.github.io/saba-80/`

---

## 🎮 שלב 3 – איך משחקים בערב

1. **המנחה** פותח את הכתובת על מסך גדול / מקרן ולוחץ **"יוצר המשחק / מנחה"**.
   נפתח **מסך עריכת שאלות** – כותבים/עורכים שאלות ישירות באתר (אפשר Ctrl+B להדגשה),
   מסמנים את התשובה הנכונה בעיגול ✓, מוסיפים שאלות עם "➕ הוסף שאלה", ולוחצים **"התחל משחק"**.
2. במסך הלובי מופיעים **קוד חדר (PIN)** ו-**QR Code**. האורחים סורקים את ה-QR בטלפון
   (או נכנסים לכתובת ומזינים את הקוד ידנית), בוחרים שם ו**דמות** (א' / ע' / ש').
3. כשהצטרפו **לפחות 2 שחקנים** – כפתור "התחלת המשחק" נפתח והמנחה לוחץ עליו.
4. לכל שאלה יש טיימר. השחקנים בוחרים צבע/צורה בטלפון. ניקוד לפי מהירות + נכונות.
5. אחרי כל שאלה: תשובה נכונה, גרף תשובות וטבלת מובילים. בסוף – מנצח וקונפטי! 🏆

---

## ✏️ עריכת השאלות
עכשיו עורכים **הכל בתוך האתר** – במסך "יוצר המשחק / מנחה". אין צורך לגעת בקוד.
האתר נטען עם כמה שאלות דמו של סבא צדוק כדוגמה; פשוט ערכו/מחקו/הוסיפו כרצונכם.
כפתור "התחל משחק" נדלק רק כשיש לפחות שאלה אחת מלאה (שאלה + 4 תשובות + סימון הנכונה). 🎂

---

## 💡 טיפים
- כדאי לבדוק מראש עם 2 מכשירים שהחיבור עובד.
- אם משהו נתקע – רעננו את מסך המנחה ופתחו משחק חדש (קוד חדש).
- הכל רץ בדפדפן, אין צורך בהתקנות.
